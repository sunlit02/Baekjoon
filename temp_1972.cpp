#include <iostream>
#include <cstring>

using namespace std;

void surprise(char c)
{
    int l;
    l = (int)strlen(c)
    for (int i = 0; i<l-2; i++)
    {
        
    }
}

int main()
{
    while (i != '*')
    {
        char i[101];
        scanf("%c", &i);
        surprise(i)
    }
}
